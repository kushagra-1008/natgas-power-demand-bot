[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# The scheduled function itself declares @hourly. Keep this file free of a
# second schedule so there is only one scheduler trigger.
